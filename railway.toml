[build]
builder = "NIXPACKS"
buildCommand = "echo BASE_DIR && ls static/ && python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate && gunicorn school_system.wsgi"